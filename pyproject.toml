[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "openclaw-trader"
version = "0.1.0"
description = "Trading agents for openclaw-trader"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
